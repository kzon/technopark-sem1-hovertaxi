#include "Order.h"

namespace components {

std::vector<models::Order> Order::LoadOrders() {
  std::vector<models::Order> orders;
  return orders;
}

models::Order Order::CreateOrder(models::GeoPoint &from, models::GeoPoint &to) {
  models::Order order;
  return order;
}

}

