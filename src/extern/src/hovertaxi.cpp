#include "components/order/Order.h"
#include "components/vehicle/Vehicle.h"

int main() {
  return 0;
}

// Just for Python integration demo
#include <string>

std::string demo(std::string s) {
  return "This is demo. You passed " + s + "!";
}